machine:
  services:
    - docker

dependencies:
  override:
    - golang -version
    - make geth
    - docker info
    - docker build --rm=false -t magnustiberius/etheriumgo .


deployment:
  hub:
    branch: master
    commands:
      - docker info
      - docker login -e $DOCKERID -u $DOCKERUID -p $DOCKERIDPWD
      - docker push magnustiberius/etheriumgo

